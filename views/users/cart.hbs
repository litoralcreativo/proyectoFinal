<nav class="navbar navbar-expand-lg navbar-dark bg-dark" style="z-index: 10; width: 100%">
  <div class="container">
    <a class="navbar-brand" href="#"><img src="/images/logogray.png" style="height: 50px" alt="logo"></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav" style="width: 100%">
        <a class="nav-link" href="/users">Inicio</a>
        <a class="nav-link" href="/users/productos">Productos</a>
        <a class="nav-link active" aria-current="page" href="/users/cart">Carrito {{#if amount}}<sup><span id="cart-amount">&nbsp{{amount}}&nbsp</span></sup>{{/if}}</a>
        <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Docs</a>
        <a class="nav-link ms-auto" href="/logout"><i class="fas fa-sign-out-alt"></i>&nbsp Logout</a>
      </div>
    </div>
  </div>
</nav>

{{!-- Background --}}
<div class="grocery2-bg" style="padding: 0; width: 100%; height: 100vh; position: fixed; top: 0; z-index: -1"></div>
<div class="bg-custom-dark-translucid" style="padding: 0; width: 100%; height: 100vh; position: fixed; top: 0; z-index: -1"></div>

<div class="container-fluid d-flex flex-row justify-content-center align-items-center my-5" style=" padding: 0; height: 100%;">
    <div class="row container bg-white custom-form">
        <div class="col">
            <table class="table table-hover text-left">
                <thead>
                    <th>Nombre</th>
                    <th>Cantidad</th>
                    <th>$/kg</th>
                    <th>Precio</th>
                </thead>
                <tbody>
                    {{#each carrito}}
                    <tr style="vertical-align: middle;">
                      <td>{{nombre}}</td>
                      <td>{{cantidad}} g</td>
                      <td>${{precio}}</td>
                      <td>${{precioFinal}}</td>
                    </tr>
                    {{/each}}
                    <tr>
                      <td></td>
                      <td></td>
                      <td></td>
                      <td><strong>${{precioTotal}}</strong></td>
                    </tr>
                </tbody>
            </table>
            <a href="/users/cart/compra" class="btn btn-custom-light mb-5"><i class="fas fa-shopping-cart"></i> Finalizar Compra</a>
        </div>
    </div>
</div>